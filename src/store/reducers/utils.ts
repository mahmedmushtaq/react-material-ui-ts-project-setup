import { ACTIONS } from "../enums";


const initialState = {
 
};

const utils = (state = initialState, actions: any) => {
  switch (actions.type) {
    
    default:
      return state;
  }
};

export default utils;
